<main>
    <ul class="feed">
        <%= render @photo %>
    </ul>

    <div id="paginate">
        <div class="container">
            <div class="row d-flex justify-content-center">
                <div class="col-sm-3">
                    <%= will_paginate @photo %>
                </div>
            </div>
        </div>
    </div>
</main>